# Vite dev server port. Used by npm run dev.
HTTP_PORT_DEV=5173

# Production server port. Used by npm run preview / server/index.mjs.
PORT=4173

# SQLite database path. Keep this file or directory persistent in production.
PAL_CONF_DB_PATH=./data/configs.sqlite
